services:
  - type: web
    name: ve-radar
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: MINIMAX_API_KEY
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
